Pricing tier card — 3-up at desktop, 2-up tablet, 1-up mobile.

```jsx
<PricingTierCard name="Growth" price="$499" cadence="/ month" featured features={['Unlimited API keys', 'Priority support']} />
```

- Highlight the recommended tier with `featured` (dark inversion). Never a coloured ribbon or badge stripe.
