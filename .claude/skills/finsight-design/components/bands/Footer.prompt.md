White-canvas closing footer — six link columns, then a hairline legal band.

```jsx
<Footer />
<Footer columns={[{ title: 'Developers', links: ['Docs', 'Status'] }]} />
```

- Footer links are body grey, not blue; only the legal strip drops to caption size.
