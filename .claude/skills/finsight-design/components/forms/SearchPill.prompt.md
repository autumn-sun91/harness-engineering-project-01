Pill-shaped search field on `--color-surface-strong`, 44px tall, borderless.

```jsx
<SearchPill placeholder="Search all assets" width={320} />
```

- Used in nav bars and above asset lists. Never gets a border or shadow.
