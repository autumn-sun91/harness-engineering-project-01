Standard 48px text field — 12px radius, hairline border that thickens to 2px Finsight Blue on focus.

```jsx
<TextInput label="Email" placeholder="you@company.com" hint="We'll never share it." />
```

- Only Default and Focus states are defined; validation styling is undocumented in the source.
