Circular plate behind an asset glyph — 32px in asset rows.

```jsx
<AssetIcon symbol="BTC" fill="var(--color-accent-yellow)" />
<AssetIcon symbol="ETH" />
```

- Accent yellow is illustrative only and appears nowhere else in the system.
